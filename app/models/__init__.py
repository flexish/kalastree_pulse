"""
Data models.

Internal representations of persisted entities (reflections, users, KPIs,
goals, milestones, ...). Kept separate from `schemas/`, which defines the
request/response shapes exposed over HTTP — models describe what we store,
schemas describe what we send and receive.
"""
